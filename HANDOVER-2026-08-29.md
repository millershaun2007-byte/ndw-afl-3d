# AFL 3D — handover, 29 Aug 2026

## State

    live        v=4de2621f38   commit 37fc761   branch approved-plus-controls
    (WRONG in the original text above this line - corrected 29 Aug, see CORRECTION at the end)
    main        a928984 (abandoned line - do NOT build on it)

`main` carries 73 commits Shaun never played. The live line branched from
`c70689e` (21 Aug, before the scoreboard) and is 20-odd commits, also mostly
unplayed.

## Reference builds — original bytes, nothing rebuilt

    afl3d-19aug-original    31,098,412   19 Aug, before the chain existed
    afl3d-prescoreboard     31,101,060   21 Aug 13:42Z, before scoreboard/quarters/finals
    afl3d-fullground        31,100,967   21 Aug 12:08Z, last build with maxChainDepth = 3
    afl3d-21aug             31,102,400   21 Aug 14:07Z, finals series

`https://storage.googleapis.com/ndw-game-builds/<slot>/index.html`

Rebuilding a commit does NOT reproduce its build - BuildSceneContents
regenerates the scene (31,102,400 archived vs 31,120,864 rebuilt).

## What Shaun last approved

`v=fd05a96687` - pre-scoreboard game with the full spoil work. Everything
after it is unplayed.

## The rule that keeps being broken

One change, one build, HE PLAYS IT, then the next. Tonight broke it
repeatedly and that is the whole reason the history is unreadable.

## Verification limit that caused real disagreement

Automated checks mash the button continuously, which always wins the ruck and
always exercises the attacking path. Shaun loses rucks and then watches. So
"verified working" has meant "the code path fires", never "this is playable".
Say which one you mean.

## Bug family - five instances tonight, all the same shape

A value computed and never read, or a constant borrowed from another beat:

    _bestHumanErr       never read      ruck unwinnable
    markBestErr         never read      mark timing ignored
    no timing test      absent          every AI kick spoiled
    shotKickHeight      borrowed        spoil flew like a set shot
    hardcoded 1.4f      vs derived 1.65 players never met in the air
    Mathf.Abs on range  dropped sign    shots taken from defence

Check reads, not just assignments. Never reuse another beat's constant.

## Open, per Shaun

- "still looks shit" - the power system is in but unproven
- losing the ruck means watching the passage; he flagged this as the reason it
  does not feel like playing
- video for speccies/spoils was discussed, not built

---

# ADDENDUM — read this before you touch anything

Added 29 Aug afternoon, after Shaun said the previous session had lost the plot
over a run of 14 commits in 67 minutes with no playtest between any of them.
The four items below are all verified against `spilled-removed` at `193f329`.

## 1. Two comments lie about the shot. Delete them.

`Day1RuckContest.cs` lines **1416** and **1710** both still say:

    TEMPORARY, at Shaun's request 2026-08-29: every shot is a goal

**That is no longer true.** `alwaysGoal` was removed and `isGoal` is real again
(line 1728, driven by `kickedInWindow`). The comments survived the code they
described. A session reading them will either hunt for a flag that no longer
exists, or conclude the shot is faked when it is not.

Delete both. No code change.

## 2. `markBestErr` is dead AGAIN — the same bug this file already warns about

    837   float markBestErr = float.MaxValue;    declared
    946   if (err < markBestErr) ...             assigned
    952   markBestErr = Mathf.Abs(...)           assigned
    ...   read by nothing

The bug table above lists "markBestErr — never read — mark timing ignored" as
one of the five defects of 28 Aug. It was fixed, and it is now broken again,
because the speccy moved onto `_clearancePower` and nothing took over reading
the timing.

Do not just note this. **It is the fix for item 3.**

## 3. Shaun has CHANGED DIRECTION on the power bar. The branch has not caught up.

His words, 29 Aug: *"yeah one button my power bar idea was getting to
complicated."*

The branch currently has the meter wired into three beats — ruck (`170`, `356`),
speccy (`495`), clearance (`616`) — with `speccyPowerMin = 0.85f`. That work was
done before he changed his mind. It is not wrong, it is superseded.

**What he actually wants now**, and it is simpler than what is there:

> No bar. `markBestErr` is already measured every frame against `markTargetT`.
> Grade it into three tiers instead of throwing it away:
>
>     very close   -> SPECCY
>     within window -> normal mark
>     outside       -> spilled
>
> One button, one tap, timing decides. Nothing new on screen.

That single change fixes item 2 and delivers item 3 at the same time — the dead
variable becomes the mechanic. `speccyChance` is already deleted (good, that was
the point); this replaces the meter that replaced it.

**Ask him before ripping the meter out of the ruck.** The ruck may be fine on a
meter even if the mark is not. Confirm, do not assume.

## 4. Nine branches. Here is what they are.

    good-19-aug             728f2be   19 Aug, the game he remembers as good
    good-pre-scoreboard     c70689e   21 Aug 23:18, last commit before the scoreboard
    production-prescoreboard c70689e  same commit, different name
    spilled-removed         193f329   THE LIVE LINE — build from this
    approved-plus-controls  37fc761   approved build + controls only
    prescoreboard-spoil     b2f47c6   run/bounce experiment, diverged
    aug22-plus-spoil        ba24084   superseded
    main                    a928984   ABANDONED — 73 unplayed commits, do not build
    claude/afl-game-...     37f3ac9   docs only: POWER-SPEC.md, REBUILD-PLAN.md

If you are unsure, `spilled-removed` is live and `main` is poison. Do not create
a tenth branch.

## 5. The rate is the actual problem

14 commits between 11:56 and 13:03. None played. The five defects in the table
above all came from exactly this, and item 2 above is a sixth, created the same
way — a variable left computed and unread while the thing that read it moved
somewhere else.

**One change. One build. He plays it. Then the next.** If you have made two
commits without a playtest between them, stop and deploy.

## What NOT to do

- Do not rebuild a commit and call it a restored build. `BuildSceneContents`
  regenerates the scene, so a rebuild is different bytes. The originals are in
  the reference slots listed above.
- Do not set `maxChainDepth` above 1 without asking. Killed twice by playtest.
- Do not raise `managedStrippingLevel` without `[Preserve]`/`link.xml` in the
  same commit. It strips `TapPressed` and the button goes dead with no error.
- Do not deploy with a bare `gsutil cp`. Use `sync-to-gcs.sh` or the build is
  invisible to anyone who has played before.


---

# CORRECTION — the "live branch" above is wrong. Read this.

Verified 29 Aug against the bucket, not against any document.

**`spilled-removed` is NOT live. `approved-plus-controls` (`37fc761`) is.**

    live stamp     v=4de2621f38        (this file originally said v=0a914cf122)
    live wasm      17,635,243
    live data      31,102,127
    ancestry       37fc761 is NOT a descendant of 193f329 - the lines DIVERGED

**Build from `approved-plus-controls`. Do not build from `spilled-removed`.**

The two branches are ~712 lines apart in `Day1RuckContest.cs` plus the scene.

## Why this matters more than a wrong name

The power meter exists ONLY on `spilled-removed`:

    approved-plus-controls    speccyPowerMin: 0    _power: 0
    spilled-removed           speccyPowerMin: 3    _power: 10

Shaun changed his mind about the power bar ("my power bar idea was getting to
complicated"). **The live branch already reflects that decision** - it is
one-button and timing-based with no meter anywhere. A session following the
original text would have built the meter back over the approved build and
shipped him the thing he had just rejected.

So item 3 of the addendum was right in substance and wrong about where you are:
you are not "about to" simplify, you already have. There is no meter to remove
from the ruck, and the ruck on the live line is already timing-graded.

## Item 2 was also wrong about the CAUSE

`markBestErr` is dead on **both** branches, live included:

    723   declared      818, 824   assigned      read: nowhere

So it is not "dead because the speccy moved onto `_clearancePower`" - the live
branch has no `_clearancePower` and it is dead there anyway. This defect
predates the power bar and **is currently shipped**: on the live build, mark
timing is genuinely ignored.

Its twin splits the other way: `_bestHumanErr` is READ on the live branch
(line 317) and dead on `spilled-removed`, where the meter took over the ruck.
So the power-bar branch created a seventh instance of the family, not the sixth.

## The three lying comments are on `spilled-removed` only

Three sites, not two - 1416, 1711, and a third at 989-1000 describing a
`markPerfectWindow` gate the code does not implement. **The live branch is clean
of all three.** Nothing to delete there.

## Branch count

Ten local branches, not nine. `one-change-at-a-time` and
`confirmed-working-2026-08-12` are missing from the map above;
`rebuild-from-scratch` exists too.

## The lesson, since it is the same one twice in two days

The original text of this file asserted a live branch without checking the
bucket. So did my addendum, which trusted it. Both were one `curl` away from
being right.

**Never state what is live from a document. Check the stamp:**

    curl -s https://storage.googleapis.com/ndw-game-builds/afl3d/index.html | grep -o 'v=[a-z0-9]*'
